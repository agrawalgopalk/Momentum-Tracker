services:
  - type: web
    name: momentum-tracker-app
    runtime: python
    plan: free
    startCommand: streamlit run app/dashboard.py --server.port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false 
